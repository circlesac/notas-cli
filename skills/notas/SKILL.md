---
name: notas
description: Operate notas, the multi-provider notes and documents CLI. Use for Notion pages, databases, blocks, users, comments, search, authentication, or notas setup and commands; load the matching provider reference before acting.
---

# notas

Identify the requested notes or documents provider, then read its reference completely before running commands:

- Notion: [references/notion.md](references/notion.md)

Use `notas --help` and `notas <provider> --help` when the installed CLI may be newer than the reference. Prefer structured output for reads and verify the resulting resource after writes.
